#pragma once 

#include "Buffer.h"

namespace Renderer::GL {

	namespace Internal {


		class BaseAttributeBuffer {

			template<typename Ty, class Buffer>
			BaseAttributeBuffer(const Buffer& vboBuffer) {
				// constexpr calculate stride
				// constexpr 
			}

		};
	}
}